# 逆引き旅行アプリ 観光スポットデータ収集方法設計
作成日：2026-07-12

「出発駅から¥X以内で行ける、知られていない景勝地」を提示するための、観光スポットデータの収集・絞り込み設計。

---

## 1. データソースと取得項目

| 項目 | 取得元 | 備考 |
|---|---|---|
| スポット名・座標・分類 | Wikidata（SPARQLクエリサービス） | `wdt:P625`（座標）、`wdt:P31`（instance of） |
| 説明文 | Wikipedia日本語版 REST API（page/summary） | Wikidataのjawikiサイトリンクから記事タイトルを特定して取得 |
| 画像 | Wikidata `wdt:P18`（image）→ Wikimedia Commons | なければWikipedia記事のサムネイルで代替 |
| 知名度（絞り込み用） | Wikimedia Pageviews API（per-article, ja.wikipedia） | 2026-07-12決定：知名度指標として採用 |

---

## 2. 収集フロー（駅を起点にした半径検索）

```
① 対象駅の座標を取得（GTFS stops.txtより）
② Wikidata SPARQLで、その座標から半径2〜3km以内のスポットを検索
     SERVICE wikibase:around を使用（center=駅座標, radius=2km等）
     P31（instance of）で観光スポット関連クラスに絞り込む（2026-07-14決定：初期リストで開始）
     - 自然系：湖・滝・山・温泉・公園
     - 歴史文化系：神社・寺・城・史跡
     - 展望系：展望台
     ※ 網羅性より実用性を優先した初期リスト。QIDの精緻な選定・誤検出の調整はVSCode側で実データを見ながら行う（このサンドボックスではWikidata自体の動作検証ができないため）
③ ヒットしたスポットごとに、jawikiのサイトリンクからWikipedia記事タイトルを特定
④ Wikipedia REST APIで説明文・画像を取得
⑤ Wikimedia Pageviews APIで直近12か月の月間平均アクセス数を取得
⑥ アクセス数が閾値未満のスポットを「隠れた景勝地」として優先表示
```

---

## 3. 「知られていない」の判定方法（2026-07-14決定：相対順位＋絶対値のハイブリッド方式）

- 指標：ja.wikipedia記事の月間平均ページビュー数（Wikimedia Pageviews API, per-article, monthly, 直近12か月平均）
- **基本ロジック**：検索結果（駅から半径2〜3km以内のスポット群）内で、アクセス数が低い順に優先表示する相対順位方式を採用。都市部・地方で母数のスケールが大きく異なるため、絶対値の閾値を1つに決め打ちするより頑健。
- **例外処理（絶対値による足切り）**：相対順位だけだと「たまたま近くに超有名スポットしかない」場合に、その中で一番マシなだけの有名スポットが「隠れた景勝地」として出てしまう。これを防ぐため、月間アクセス数が突出して多い（後日、実データを見ながら具体的な閾値を設定）スポットは、相対順位に関わらず一律で除外する。
- 記事はあるがページビューデータが存在しない（新設・低頻度記事等）場合 → 「知名度データなし＝隠れている扱い」としてデフォルトで優先表示側に倒す
- Wikipedia記事が存在しない（Wikidata項目のみ）スポットの扱いは要検討（説明文が作れないため、そもそも表示対象から除外するか、Wikidataの説明文フィールドで代替するか）

---

## 4. 実装方式：ビルド時バッチ処理（ライブAPI呼び出しはしない）

単一HTMLファイル・バックエンドなしという技術方針のため、上記①〜⑤の収集処理は**アプリの実行時（ブラウザ）ではなく、事前のビルド時バッチ処理として1回だけ実行**し、「駅ID→周辺スポットリスト（説明文・画像URL・アクセス数込み）」の静的JSONとして生成、アプリに同梱する。

理由：
- 検索のたびに複数の外部APIをブラウザから叩くと低速・不安定
- Wikidata/Wikipedia/PageviewsのAPIはレート制限があり、多数ユーザーの同時アクセスに耐えない
- 静的JSON化すればGitHub Pagesでのホスティングと相性が良く、オフラインでも動作する

---

## 5. 未検証・未決定事項

- [ ] このCoworkサンドボックス環境ではWikidata SPARQL・Wikimedia Pageviews APIともに動作検証できなかった（JSON系APIの応答が空になる制約）。VSCode側の開発環境で実クエリの動作確認が必要
- [ ] P31クラスの初期リスト（自然系・歴史文化系・展望系）を実データで検証し、誤検出があれば調整
- [ ] 「絶対値による足切り」の具体的な閾値を実データを見ながら設定
- [ ] 全国の駅数に対して、このバッチ処理をどの程度の時間・コストで回せるか（API呼び出し回数の見積もり）
