# reverse-travel（逆引き旅行アプリ）

「出発駅と予算を入れると、その予算内で公共交通機関だけで行ける**知られていない景勝地**を返す」アプリ。

- 静的サイト＋事前生成JSON（バックエンドなし、GitHub Pages）
- 公開URL: https://minami610.github.io/reverse-travel/ （main branch / `docs` フォルダ）
- 交通手段は公共交通機関のみ（バス＋電車）、日帰り前提、予算の定義は**交通費のみ**

---

## コンテスト応募条件（最重要の制約）

**公共交通オープンデータチャレンジ2026**（応募開始 2026-10-01、締切 2027-01-11）に応募予定。

- **データ取得元は「公共交通オープンデータセンター」または「GTFSデータリポジトリ」に限られる。**
  現在は **gtfs-data.jp API v2** を使用（条件を満たす）。
  当初使っていた「ことでん」の直接取得はどちらにも登録されておらず応募不可だった。**この制約を外れるデータ源を追加しないこと。**
- 国土地理院はオープンデータ・パートナーであり、地理院タイルの使用は加点要素。
- **アプリに必須の表示3点**（未実装なら最優先で実装すること）
  1. データの出典（公共交通オープンデータセンター／gtfs-data.jp提供である旨。CC BYのフィードは各公開元も明記）
  2. データの正確性・完全性が保証されていない旨
  3. 開発者の問い合わせ先（交通事業者へ直接問い合わせないよう促す一文を含む）
- 審査基準：①社会課題解決 ②オープンデータ活用のインパクト ③技術的完成度 ④UI/UXの完成度。
  **都道府県のカバレッジ数は②にしか効かない。半分壊れた全国版は③④を直撃する。カバレッジより完動を優先する。**

---

## 現在の状態

香川県版が公開済み。**全国対応（約43都道府県）へ移行中。**
鳥取・広島・宮崎・福井の4県は両ソースともデータがないため、カバレッジは「約43都道府県」と正確に表記すること。

### 主要ファイル

```
scripts/
  fetch-and-build.js                本番ビルドの入口。※まだ旧方式(generate-spots.json.js)を
                                    呼んでおり、新パイプラインに未接続
  bundle-html.js                    単一HTMLバンドラ（docs/へ自動コピー）
  verify-initial-render.js          jsdomによる描画検証（npm run verify-render）
  build-pipeline/
    fetch-gtfs.js                   GTFS取得＋解凍＋必須ファイル検証
    parse-and-transform.js          GTFS→派生JSON（zone_id→stop_id変換、名前空間化、整数インデックス）
    generate-route-details.js       直通検証・所要時間中央値・期待待ち時間
    generate-spots-by-region.js     ★現行のスポット収集（bbox一括取得・除外機構・県判定）
    generate-spots.json.js          旧方式＋Wikipedia enrichment関数群
    generate-municipality-table.js  市区町村→都道府県の対応表を生成
assets/js/  main.js / gtfs-loader.js / fare-calculator.js / spot-finder.js /
            route-formatter.js / route-duration.js / map-view.js / layout-controller.js
config/     target-operators.json / spot-config.json / spot-ranking-config.json
```

### 設計上の決定（経緯は git log に詳しい）

- **運賃**：GTFSは①均一運賃型 ②OD運賃表型の2パターン。**データから自動判定する**（configを信用しない）。
  `fare_rules.origin_id`/`destination_id` は **`zone_id` であって stop_id でも駅名でもない**。
  運賃非対応のバス路線は除外。経路選択は「運行頻度の下限＋所要時間最短」で、**待ち時間（運行時間帯÷便数÷2）を考慮しないと結果が逆転する**。
- **スポット収集**：Wikidataのbbox（`wikibase:box`）で地域一括取得。停留所ごとの`wikibase:around`は全国規模で破綻する（5.5〜9日）。
  P31は `wdt:P31/wdt:P279*` でサブクラスを辿る。記事タイトルは**テキスト検索ではなくWikidataのサイトリンクから**取得する。
- **知名度指標は Wikidataの `wikibase:sitelinks`**（pageviewsは廃止）。
  pageviewsは在校生アクセス等で観光地の知名度を反映しない（例：石川県立金沢二水高校が県内最高値級）うえ、レート制限で富山1県に174分かかった。
  sitelinksは低い値に密集し55%が同順位のため、**`sitelinks >= 10` の「足切り専用」**とし、順位付けは**情報充実度（本文文字数→画像の有無）**を主軸にする。
- **除外機構は4層**（`config/spot-config.json` に全定義。ハードコード禁止）
  1. **除外優先ルール**：「除外クラス該当 **かつ** 守護クラス非該当」の場合のみ除外
  2. **ガードテスト（ベースライン方式）**：除外QID×守護QIDをP279\*で総当たり。既知の衝突は`guard_test_approved_conflicts`に理由付きで記録して通し、**未知の衝突だけビルドを停止**
  3. **誤除外の機械的検出（常設）**：除外セットのうち「記事あり＋画像あり＋sitelinks>=3」を抽出。学校や変電所は画像を持たないため、これを通るものは誤除外の疑いが濃い
  4. **個別許可リスト（QID単位）**：クラス保護が広すぎる場合の精密ツール
- **除外の判断軸**：**「住民のための施設」は除外、「景色になりうるもの」は残す。除外はタダのとき（巻き添えゼロのとき）だけ行う。**
  除外は取り返しがつかないが、ランキング下位なら実害がない。
  例：発電所(Q159719)は**意図的に除外しない**（発電用ダムが下位クラスにあり黒部ダム等を巻き込むため）。橋・川・ダムも残す。
- **データ構造**：GTFSの stop_id/route_id はフィード内でしか一意でないため `{operator_id}:{raw_id}` で名前空間化必須。
  文字列IDは県別辞書＋整数インデックスに置換してサイズを削減（構造系58%減、spots 65%減）。
- **フロントエンドは QID で重複排除する**（`gtfs-loader.js` の `mergeIndexedSpotRegions()`）。
  隣接県を同時ロードする設計のため、複数県にまたがるスポットや県判定フォールバックによる重複を吸収する。

---

## 絶対に守るルール（過去に実害が出たもの）

- **構文チェックが通っても画面は壊れる。描画不能が過去3回発生している。**
  フロントエンドやデータ形式を触ったら必ず `npm run verify-render`（初期描画＋検索後の状態まで検証）。
- **各段階で件数をログし、0件なら成功扱いにせず失敗させること。**
  過去にZIP未解凍で全JSONが2バイトなのに「成功」と報告した事故がある。
- **整数インデックスの0はfalsy。`if (!id)` は「なし」と誤判定する。`=== undefined` で判定すること**（過去に13路線が消えた）。
- **SPARQLで GROUP BY / DISTINCT を書かないとデカルト積で件数が水増しされる**（複数座標・複数画像を持つ項目が行数分だけ重複）。
- **「最初の1行を採用」はSPARQLの行順が保証されないため非決定的**。ビルドごとに座標が変わりgit diffが汚れる。決定的な選択規則を使うこと。
- **Wikidataの抽象クラスは予想外のものを包含する**（行政区画→城、企業→温泉、集落→修道院・花街）。除外リストを触ったら必ずガードテストを通すこと。
- **WDQSの応答時間は同一クエリでも13秒〜51秒〜504タイムアウトまで振れる。1回の計測で性能を結論づけないこと。**
- **高速バスの停留所（バスタ新宿・梅田等）がbboxを暴走させる。** GTFSフィードは県単位だが高速バスは他県の停留所を含むため、bbox計算前に中央値ベースの外れ値除外が必須（実装済み）。
- **「日本の市(Q494721)」だけでは県庁所在地が漏れる**（富山市・金沢市は中核市／都道府県庁所在地クラスを持つ）。
- UTF-8 BOM付きの `stops.txt` には `bom: true` が必要。
- `LIMIT` は `ORDER BY` なしで使わない（任意のN件に切られる）。
- 地図・位置図の画像（`Japan_Map_Lincun.svg` 等）が混入するため正規表現で除外する。
- **計測データはサンドボックス削除前に退避すること**（174分かけた計測結果を検証前に消した事故がある）。

## 作業の進め方

- **大きなビルドは再開可能にすること。** 県単位の完了マーカー、既完了県のスキップ、強制再取得オプション。1県失敗でも全体を止めず最後に失敗一覧を出す。ただし**件数0での「成功」は失敗として扱う**。
- **全国一括ビルドはやらない。** まず少数県（隣接ペアを必ず含む）で全経路を通してから展開する。隣接県同時ロードの罠は2県目以降でしか露見しない。
- **意味のある単位でこまめにコミットすること。** コミットメッセージには「何をしたか」だけでなく**「なぜそうしたか（採用しなかった選択肢と理由）」**を書く。このリポジトリでは git log が設計記録を兼ねている。
- ODPTアクセストークンは `.env` に置き、チャットに貼らない（`.gitignore` 登録済み）。アカウント作成・パスワード入力は本人が行う。
- `data/derived/` は `.gitignore` 対象。生成物がない状態で静かに壊れないよう、必須の生成物は不在時にエラーで停止させること。
