# 逆引き旅行アプリ 運賃計算ロジック設計
作成日：2026-07-12

実データ（清須市あしがるバス／ことでん）の検証結果を踏まえた、「出発駅から¥X以内で行ける場所」を求めるアルゴリズム設計。

---

## 1. 実データから確認された運賃パターン

GTFSの運賃データは、事業者によって以下の2パターンに大別される。両方とも `fare_rules.txt` に記載されるが、埋まっている項目が異なる。

| パターン | 該当した実例 | fare_rules.txtの形 | 運賃の決まり方 |
|---|---|---|---|
| ①均一運賃型 | 清須市あしがるバス | `route_id`のみ記載、`origin_id`/`destination_id`は空欄 | その路線に乗ったら区間によらず定額 |
| ②OD運賃表型 | ことでん（鉄道） | `origin_id`/`destination_id`を記載、`route_id`は空欄 | 駅名ペアごとに運賃が個別に列挙済み（53駅×52駅＝2,756件、双方向とも記載） |

**重要な発見**：②のOD運賃表型は、駅間の距離から運賃を計算する必要がない。全ペアの運賃がすでに列挙済みのため、「引くだけ」で済む。運賃計算エンジンを自作する必要はなく、**参照テーブルの検索問題**に帰着する。

---

## 2. 設計上の本当の難所

運賃そのものの計算は上記の通り単純だが、以下2点がこのアプリ特有の難しさになる。

### (a) 事業者をまたぐ乗り継ぎ
GTFSのfare_rulesは事業者（フィード）ごとに閉じている。電車→バスのように事業者をまたぐ移動は、各事業者の運賃を個別に引いて合算する必要がある。かつ、どの駅・停留所が「乗り換え地点」として物理的に同じ場所を指すかは、GTFS上は自動的にはわからない（事業者ごとにstop_idの体系が別なので、駅名・座標による名寄せが必要）。

### (b) 同一事業者内でも路線をまたぐ場合
あしがるバスの`transfers=0`（乗換不可）が示す通り、同一事業者内でも路線をまたぐと運賃が再度発生するケースがある。単純に「1路線＝1運賃」と決め打ちできない。

---

## 3. アルゴリズム設計：運賃グラフ探索

### 全体方針
「出発駅から¥X以内で行ける場所」は、**重み付きグラフの最短コスト探索**（ダイクストラ法）として扱う。

```
ノード：停留所・駅（事業者ごとのstop_id）
エッジ：
  ①OD運賃表型 → fare_rules.txtの行がそのままエッジ（例：高松築港-瓦町 = ¥200）
  ②均一運賃型 → 同一route_idに属する停留所同士を、fare_attributes.priceの重みで接続
      ※stop_times.txt / trips.txtから「そのroute_idが通る停留所リスト」を先に抽出して展開する
  ③乗換エッジ → 異なる事業者・異なるstop_idでも同一の物理駅とみなせる場合、重み¥0で接続
      ※駅名の完全一致・部分一致＋座標の近接（例：半径200m以内）で名寄せする
```

出発駅をスタート地点に、コストが予算¥Xを超えた時点で探索を打ち切るダイクストラ法を実行し、到達可能な全停留所とその最小コストを求める。その停留所の徒歩圏内にあるWikidata観光スポットを結果として表示する。

### 均一運賃型のエッジ展開についての注意
路線上の停留所N個すべてを愚直に総当たり（N×N本のエッジ）で展開すると、大規模事業者でエッジ数が膨れる。実装上は「停留所→仮想の路線ノード（コスト0）→停留所（コストは運賃）」という中間ノードを挟む形にすると、エッジ数がO(N)に抑えられる。

---

## 4. コンテスト提出（フェーズ1）向けの実装範囲の絞り込み提案

上記の汎用グラフ探索は、事業者をまたぐ乗換駅の名寄せ（3-(a)）が最も工数のかかる部分であり、全国規模でこれを完全にやり切るのはコンテスト応募期間（〜2027年1月11日）を考えると現実的でない可能性がある。そこで、実装を2段階に分けることを提案する。

| フェーズ | スコープ | 実装難易度 |
|---|---|---|
| **フェーズ1（コンテスト提出版）** | 出発駅から**単一事業者（主に鉄道）のOD運賃表を直接検索**し、到達可能駅を算出。さらに、その到達駅から**乗換なしで乗れるコミュニティバス1本分**を「ラストワンマイル」として加算（＝乗り継ぎは最大1回までに限定）| 低〜中（グラフ探索は不要、直接ルックアップ＋簡易な1回乗換のみ） |
| **フェーズ2（本開発・コンテスト後）** | 任意の乗換回数・任意の事業者組み合わせに対応した汎用グラフ探索。全国の駅・停留所の名寄せデータベースを整備 | 高 |

フェーズ1は「予算内で行ける、知られていない景勝地」というコンセプトの体験価値を損なわずに、開発期間内で実現可能な現実的なスコープと考える。

---

## 6. 運賃非対応バス路線の扱い（2026-07-14決定）

**除外する方針で決定。** 国土交通省の全国調査（2025年4月8日時点、GTFS公開データ967件＝バス898・鉄道18・フェリー51）では、fare_attributes.txtの主要フィールドの採用率が約95%と、運賃非対応の路線は少数派であることが確認できている。少数のレアケースのために代替案内ロジック（除外以外の対応）を作り込むより、単純に「運賃データが取れない路線は到達可能スポットの計算から除外する」方針でシンプルに保つ。理由は2つ：
- 頻度が低く、複雑なフォールバックロジックへの投資対効果が低い
- 「予算内で行ける」が前提のアプリで、運賃不明区間を無理に含めるとユーザーの信頼を損なうリスクの方が大きい

---

## 7. 未解決事項（次のアクション）

- [ ] 駅名寄せ（同一物理駅の判定）に使うデータソースの選定：国土交通省の駅座標データ or 独自にWikidata座標を使うか
- [ ] 均一運賃型で、同一事業者内の路線またぎ（3-(b)）をフェーズ1でどう扱うか（対象外とする／乗換不可の路線バスは考慮しない、等の割り切りが必要）
- [ ] fare_attributes.transfers / payment_methodフィールドの値ごとの扱いをコード上でどう分岐するか
- [ ] 実際にことでんの53駅データを使ったプロトタイプ（¥500以内で行ける駅の一覧を出す等）で動作検証
